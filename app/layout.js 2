import './styles.css';

export const metadata = {
  title: 'SchoolFlow',
  description: 'School schedule, assignments, and study planning in one place.'
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
